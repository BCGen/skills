# Tasks: refine-retro

## 1. Rename and refine

- [x] 1.1 Rename `skills/task-retrospective/` → `skills/retro/` (frontmatter name, directory) and update every reference (add-rule SKILL.md, ai-init playbook + learnings README template, repo README, repo CLAUDE.md block text, living spec folder, tests paths)
- [x] 1.2 Apply A (per-proposal reasoning + decline consequence), B (fifth route: project fact → entry-file diff-first edit), C (mention install at most once), D (reconciliation scan before promotion proposals), and the light prose trim to `skills/retro/SKILL.md`
- [x] 1.3 Update personal environment: `~/.claude/CLAUDE.md` mandate line and the skills symlink

## 2. Tests and publication

- [x] 2.1 Add acceptance scenarios: reconciliation (existing rule covers candidate → mark-promoted proposal, no duplicate) and entry-file fact (corrected build command → diff-first entry edit); update tests README
- [x] 2.2 Re-run: correction, E2E recurrence-approve (add-rule installed), and the two new scenarios; verify mechanically
- [x] 2.3 Lint, commit, archive change, sync develop and push per the publishing rule
