# Tasks: press-the-premise

## 1. The premise pass

- [x] 1.1 SKILL.md: add the premise pass before the interrogation — list the nouns of
      the opening request and offer each one concrete alternative, once
- [x] 1.2 SKILL.md: settle divergent / convergent / both-as-ordered-phases there
- [x] 1.3 `references/eliciting.md`: the first thing pressed is the request, not the
      answers — with the failure that bought the rule

## 2. Invalidation

- [x] 2.1 SKILL.md: a premise the user overturns returns the flow to the checkpoint;
      name what is void, what survives, renegotiate; do not edit the draft to fit

## 3. Fit and verify

- [x] 3.1 Keep SKILL.md within both caps — move detail to references rather than
      squeezing prose
- [x] 3.2 `tests/skill-writing/README.md`: a request naming an artifact — check an
      alternative was offered before the procedure was interrogated
- [x] 3.3 A scenario where a premise is overturned — check the flow returns to the
      checkpoint and the draft is not silently reshaped
- [x] 3.4 Run `pnpm lint`; validate the change
