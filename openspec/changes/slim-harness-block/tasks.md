# Tasks: slim-harness-block

## 1. Implement

- [x] 1.1 playbook.md: block template becomes the single loop line; the
      slug-resolution paragraph is removed
- [x] 1.2 CLAUDE.md: this repo's own block converges to the new template
- [x] 1.3 tests converge scenario: preseed the old 3-line block, expect
      the current 1-line template after the run

## 2. Verify and wrap up

- [ ] 2.1 `pnpm lint`; `openspec validate slim-harness-block`; archive
      keeping the delta; publish on the user's go-ahead
