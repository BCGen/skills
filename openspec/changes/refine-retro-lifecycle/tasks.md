# Tasks: refine-retro-lifecycle

## 1. Update the skill

- [x] 1.1 Update `references/loop-file-formats.md`: status set (`candidate` / `promoted` / `resolved` / `dismissed`), promoted kept + tracks re-promotion, resolved deletes the file, root-cause H1 titling
- [x] 1.2 Update `skills/retro/SKILL.md` Step 2 (root-cause titles; stage-vs-fix-now recommended by evidence, user decides; fix-now promotes directly keeping the file)
- [x] 1.3 Update SKILL.md Steps 3–4 (recurrence shows the trail and offers cure / harden / observe; resolved marks + deletes; keep within the body line budget)

## 2. Verify and wrap up

- [x] 2.1 Acceptance via skill-testing: recurrence + user-confirms-cured → file deleted; fix-now → promoted without observe, file kept; a continued fix matches an existing root-cause candidate
- [x] 2.2 Lint, archive change, sync develop and push per the publishing rule
