# Conventions

Tool-enforced parts are not repeated here; this file only records
judgment-based conventions.

- Components are layered under `src/components/`; wrappers add behavior,
  base components stay style-only.
- Branch naming has no fixed convention.
